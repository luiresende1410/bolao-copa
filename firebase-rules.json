{
  "rules": {
    "admin": {
      ".read": true,
      ".write": "auth != null",
      ".validate": "newData.isString() && newData.val().length <= 200"
    },
    "apostadores": {
      ".read": true,
      "$nome": {
        ".write": "auth != null",
        ".validate": "newData.isString() && newData.val().length <= 200"
      }
    },
    "palpites": {
      ".read": true,
      "$apostador": {
        ".write": "auth != null",
        "$matchId": {
          ".validate": "newData.hasChildren(['home', 'away']) && newData.child('home').isNumber() && newData.child('away').isNumber() && newData.child('home').val() >= 0 && newData.child('home').val() <= 99 && newData.child('away').val() >= 0 && newData.child('away').val() <= 99"
        }
      }
    },
    "results": {
      ".read": true,
      ".write": "auth != null",
      "$matchId": {
        ".validate": "newData.hasChildren(['home', 'away']) && newData.child('home').isNumber() && newData.child('away').isNumber() && newData.child('home').val() >= 0 && newData.child('home').val() <= 99 && newData.child('away').val() >= 0 && newData.child('away').val() <= 99"
      }
    },
    "penaltis": {
      ".read": true,
      ".write": "auth != null",
      "$matchId": {
        ".validate": "newData.isString() && (newData.val() === 'home' || newData.val() === 'away')"
      }
    },
    "knockoutTeams": {
      ".read": true,
      ".write": "auth != null",
      "$matchId": {
        ".validate": "newData.hasChildren(['home', 'away']) && newData.child('home').isString() && newData.child('home').val().length <= 30 && newData.child('away').isString() && newData.child('away').val().length <= 30"
      }
    },
    "grupos": {
      ".read": true,
      ".write": "auth != null",
      "$grupo": {
        ".validate": "newData.isBoolean()"
      }
    },
    "apostadorGrupos": {
      ".read": true,
      ".write": "auth != null",
      "$apostador": {
        ".validate": "newData.isString() && newData.val().length <= 25"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
